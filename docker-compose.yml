services:
  app:
    container_name: init7tv
    image: kalinkasolutions/init7tv:latest
    ports:
      - "7880:5001"
    restart: always
    volumes:
      - ./data:/var/srv
    environment:
      - PROXY_ADDRESS=10.10.0.20